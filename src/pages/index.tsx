import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => (
  <>
    <Head>
      <title>Runpod Console</title>
      <meta
        name="description"
        content="Use runpod console to deploy and manage your GPU workloads."
      />
    </Head>
  </>
);

export default Home;
